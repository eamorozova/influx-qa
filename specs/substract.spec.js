test('1 - 2 to equal -1', () => {
  expect(1 - 2).toBe(-1);
});

test('3 - 2 to equal 1', () => {
  expect(3 - 2).toBe(1);
});

test('adds 3 - 4 to equal 9', () => {
  expect(3 - 4).toBe(9);
});

test.skip('skipped example', () => {
  expect(3 - 2).toBe(5);
});